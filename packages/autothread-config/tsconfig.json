{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // File Layout
    "rootDir": "./src",
    "outDir": "./dist",
    "declarationDir": "./@types",

    // Types
    "types": ["node"],

    "paths": {
      "@routines": ["./src/routines.d.ts"]
    }
  }
}
